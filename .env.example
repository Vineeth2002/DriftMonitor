# DriftMonitor environment variables
# No secrets are required for default operation

# Timezone for reports (optional)
TIMEZONE=UTC

# Optional: override sentiment model
SENTIMENT_MODEL=distilbert-base-uncased-finetuned-sst-2-english

# Optional: data retention days (future use)
DATA_RETENTION_DAYS=365
