# Example environment variables for DriftMonitor

# Optional YouTube API key if enabling youtube collector
YOUTUBE_API_KEY="REPLACE_ME"

# Optional model override for SafetyClassifier
SAFETY_MODEL_NAME="distilbert-base-uncased-finetuned-sst-2-english"

# If integrating third-party APIs, set tokens here (never commit real secrets)
